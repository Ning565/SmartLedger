<?xml version="1.0" encoding="utf-8"?>
<!--
  只监听微信与支付宝的页面事件（方案 2.3）：
  - packageNames 白名单：其它 App 的无障碍事件不会进本服务
  - 只订阅 WINDOW_STATE_CHANGED / WINDOW_CONTENT_CHANGED 两种类型
  - notificationTimeout=300：同类型事件的系统级节流
  - 不使用 typeAllMask、不监听所有 App（耗电红线）

  ⚠ packageNames 必须单行无空白：AOSP 的 AccessibilityServiceInfo
    用 split("(\\s)*,(\\s)*") 解析，只吃逗号两侧空白、不 trim 首尾——
    多行缩进会让第一个包名带前导空格而永不匹配（微信侧静默失效，P0-1）。
    accessibilityEventTypes 编译为 int flag，不受空白影响，可多行。

  ⚠ accessibilityFlags 的每一项都必须是真实生效的（debug.4 真机教训）：
  - flagRetrieveInteractiveWindows：AccessibilityService.getWindows() 的硬前提。
    缺它时 getWindows() 直接返回空列表且不抛异常 —— debug.3 加的「多窗口遍历
    兜底」（微信支付结果页是独立 Window，rootInActiveWindow 未必指向它）因此
    完全空转，三场景全部不记账。
  - flagReportViewIds：诊断里要展示 viewId 用于校准。
  这几项由 tools/verify_a11y_config.py 对编译产物校验，防止再次静默失效。

  ⚠ debug.10 起**刻意去掉了** flagIncludeNotImportantViews。
  它是 debug.5 按「微信自绘页可能把节点标成 not-important」**推测**加的，没有实证。
  排除法把嫌疑收到它头上：
    debug.4（只有 flagReportViewIds + rootInActiveWindow）→ 树非空（读得到聊天页）
    debug.9（三个 flag     + rootInActiveWindow）→ 树全空
  节点来源已被 debug.9 证伪，两个 flag 就只剩它没被单独验证过。
  若这一包读到了文本，就是它；若仍是空树，下一个变量是
  flagRetrieveInteractiveWindows（届时 getWindows() 与原始窗口清单会一起失效）。
-->
<accessibility-service
    xmlns:android="http://schemas.android.com/apk/res/android"

    android:description="@string/accessibility_payment_description"

    android:accessibilityEventTypes="
        typeWindowStateChanged|
        typeWindowContentChanged"

    android:accessibilityFeedbackType="feedbackGeneric"

    android:packageNames="com.tencent.mm,com.eg.android.AlipayGphone"

    android:notificationTimeout="300"

    android:canRetrieveWindowContent="true"

    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows" />
